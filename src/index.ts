const str = 'string';
const log = (str: string) => {
  console.log(str);
};
const arr = [1, 3, 4];

window.addEventListener('load', () => {
  console.log(`Page loaded!`);
});

const dic = new WeakMap();
